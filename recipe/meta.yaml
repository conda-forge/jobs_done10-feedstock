{% set name = "jobs_done10" %}
{% set version = "1.7.1" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/ESSS/jobs_done10/archive/{{ version }}.tar.gz
  sha256: 31e40396e4572a4926227516603d8c7b1321fbb86cccdf6f7abccbd192cd4320

build:
  noarch: python
  number: 0
  entry_points:
    - jobs_done=jobs_done10.cli:jobs_done
  script:
    - export SETUPTOOLS_SCM_PRETEND_VERSION={{ version }}
    - '{{ PYTHON }} -m pip install . --no-deps --ignore-installed --no-cache-dir -vvv '

requirements:
  host:
    - pip
    - python >=3.6
    - setuptools_scm
  run:
    - click
    - flask
    - mailer
    - pygments
    - python >=3.6
    - python-dotenv
    - python-jenkins
    - pyyaml

test:
  requires:
    - git
    - pytest
    - pytest-mock
    - requests-mock
  imports:
    - jobs_done10
  source_files:
    - tests
  commands:
    - jobs_done --help
    - pytest tests

about:
  home: https://github.com/ESSS/jobs_done10
  license: MIT
  license_family: MIT
  license_file: LICENSE
  summary: Job's Done uses a .jobs_done.yaml file in your repository to create Continuous Integration jobs on Jenkins.

extra:
  recipe-maintainers:
    - igortg
    - nicoddemus
    - tadeu
    - tarcisioe
