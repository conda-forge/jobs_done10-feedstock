{% set name = "jobs_done10" %}
{% set version = "1.9.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/ESSS/jobs_done10/archive/{{ version }}.tar.gz
  sha256: a8fb1a90f38ab29a2f3a2d7e0d4c958e4dfdbc795db6f8d1b5d947a39a2d4c40

build:
  noarch: python
  number: 0
  entry_points:
    - jobs_done=jobs_done10.cli:jobs_done
  script:
    - export SETUPTOOLS_SCM_PRETEND_VERSION={{ version }}
    - '{{ PYTHON }} -m pip install . --no-deps --ignore-installed --no-cache-dir -vvv '

requirements:
  host:
    - pip
    - python >=3.6
    - setuptools_scm
  run:
    - click
    - flask
    - gunicorn
    - mailer
    - pygments
    - python >=3.6
    - python-dotenv
    - python-jenkins
    - pyyaml

test:
  requires:
    - git
    - pytest-mock
    - pytest-regressions
    - requests-mock
  imports:
    - jobs_done10
  commands:
    - jobs_done --help

about:
  home: https://github.com/ESSS/jobs_done10
  license: MIT
  license_family: MIT
  license_file: LICENSE
  summary: Job's Done uses a .jobs_done.yaml file in your repository to create Continuous Integration jobs on Jenkins.

extra:
  recipe-maintainers:
    - igortg
    - nicoddemus
    - tadeu
    - tarcisioe
